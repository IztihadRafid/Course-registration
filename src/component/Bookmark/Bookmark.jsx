
const Bookmark = ({bookmark}) => {
    const {title,price} = bookmark
    return (
        <div>
            <h1>{title}</h1>
            <hr />
            
        </div>
    );
};

export default Bookmark;